<% if object.errors.any? %>
  <div class="alert alert-danger">
    <h4><%= pluralize(object.errors.count, "error") %> prohibited this record from being saved:</h4>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
